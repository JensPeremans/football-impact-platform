# Football Impact Platform - Python dependencies
# Pinned to versions verified on Streamlit Community Cloud (Python 3.11/3.12).
# Lower bounds chosen for stability; upper bounds avoid untested major releases.

streamlit>=1.30,<2.0
pandas>=2.0,<3.0
plotly>=5.18,<6.0
pdfplumber>=0.11,<0.12
openpyxl>=3.1,<4.0
scipy>=1.11,<2.0
bcrypt>=4.0,<5.0
